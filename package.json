{
  "name": "gfc-accountability-explorer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Read-only ETHOnline 2026 Continuity build for the GFC Accountability Explorer.",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "node server/index.js",
    "start": "node server/index.js",
    "build": "node scripts/build.mjs",
    "lint": "node scripts/lint.mjs",
    "test": "node --test tests/*.test.js",
    "security:check": "node scripts/security-audit.mjs",
    "check": "npm run lint && npm test && npm run build && npm run security:check"
  }
}
